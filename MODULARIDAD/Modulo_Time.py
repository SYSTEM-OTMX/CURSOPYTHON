from datetime import datetime as d


d.now()   #devuelve el year, mes, dia, hora, minutos, segundos

dt = d.now()

dt.year #devuelve solo el year

dt.month    #devuelve solo el mes

dt.day  #devuelve solo el dia

dt.hour #devuelve solo la hora

dt.minute   #devuelve solo los minutos

dt.second   #devuelve solo los segundos

dt.microsecond  #devuelve solo los microsundos